{
  "name": "firestore-timestamps",
  "version": "0.0.1",
  "author": "Anish Karandikar <anishkny@gmail.com>",
  "license": "MIT",
  "scripts": {
    "pretest": "rm -rf coverage/ .nyc_output/ && ./generate-extension-yaml.js",
    "test": "nyc firebase ext:dev:emulators:exec --test-params=test-params.env --ui 'mocha functions/*.test.js'"
  },
  "devDependencies": {
    "firebase-tools": "^9.12.1",
    "js-yaml": "^4.1.0",
    "mocha": "^9.0.1",
    "nyc": "^15.1.0"
  }
}
